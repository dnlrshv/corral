# Example instruction kernel

- Changes to payments-config MUST include a rollback note.
- Changes to payments-config MUST NOT bypass staged validation.
- ASK the service owner before changing an orders-api response field.
- READ the orders-api compatibility notes first when changing request validation.
